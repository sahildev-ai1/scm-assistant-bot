# SCM Assistant — minimal footprint for Render 512 MB
# Requires Python 3.11 (set in render.yaml) — pandas has no wheel for 3.14

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1
requests==2.32.2
python-dotenv==1.0.1

# Vector store
qdrant-client==1.9.1

# PDF parsing — lightweight, no ML
PyMuPDF==1.24.3

# Data — pre-built wheel exists for Python 3.11
pandas==2.2.2
numpy==1.26.4

# Frontend
streamlit==1.35.0
