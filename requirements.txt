# SCM Assistant — minimal footprint for Render 512 MB
# No torch, no sentence-transformers, no heavy ML libs
# Embeddings via Ollama Cloud API (HTTP only)

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.1
requests==2.32.2
python-dotenv==1.0.1

# Vector store
qdrant-client==1.9.1

# PDF parsing — lightweight C extension, no ML
PyMuPDF==1.24.3

# Data
pandas==2.2.2

# Frontend
streamlit==1.35.0
